<h2>Links</h2>
<%= strip_links("<a href='http://www.google.com'>Google</a>") %>
<hr />

<h2>Tags</h2>
<%= strip_tags("<a href='http://www.google.com'>Google</a>") %>
<hr />

<h2>Sanitize, to remove links and tgs</h2>
<%= sanitize("<a href='http://www.google.com'>Google</a>") %>
<hr />

<h2>Sanitize with options</h2>
<p> allow a tag: <%= sanitize("<a href='http://www.google.com'>Google</a>", tags: %w(a)) %> </p>
<p> allow a tag and href attribute: <%= sanitize("<a href='http://www.google.com'>Google</a>", tags: %w(a), attributes: %w(href)) %> </p>
<hr />

<h2>More examples</h2>

<%# evil_string = "<script>alert('Gotcha!');</script>" %>
<% evil_string = "<script>alert('Gotcha!');</script>".html_safe %>
<% good_string = "<strong>Welcome to my site!</strong>".html_safe %>

<%# raw(evil_string) %><br />
<%# raw(good_string) %><br />

<%= evil_string.html_safe? %><br />
<%= good_string.html_safe? %><br />

<%# h(evil_string) %><br />
<%# h(good_string) %><br />

<hr />

<% simple_string = '<strong>Please</strong> visit <a href="http://example.com">us</a>.' %>

<%= strip_links(simple_string) %>
<%= strip_tags(simple_string) %>

<hr />

<%= sanitize(evil_string) %><br />
<%= sanitize(simple_string, :tags => []) %><br />
<%= sanitize(simple_string, :tags => ['strong', 'em', 'a']) %><br />